# TODO: temporary dev-only flags; remove before production

# Enable mock login page (credentials: admin / admin123)
VITE_APP_USE_MOCK_LOGIN=true

# Skip login entirely and auto-login as admin
VITE_APP_SKIP_LOGIN=true
